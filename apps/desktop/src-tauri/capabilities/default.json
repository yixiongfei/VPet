{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Pet 窗口的最小权限：Phase 0 只需要拖动窗口",
  "windows": ["pet"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
