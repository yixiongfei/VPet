{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Pet 窗口的最小权限：提起时移动窗口 + 订阅 Core 事件",
  "windows": ["pet"],
  "permissions": [
    "core:default",
    "core:window:allow-set-position"
  ]
}
