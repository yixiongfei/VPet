# 08 · 待确认问题

每个问题都附了**我的默认假设**——你不回答我就按默认走；回答了我就更新对应文档。按"影响 Phase 0 的程度"排序。

## ✅ 已确认（2026-09-17）

| # | 问题 | 结论 | 影响 |
|---|---|---|---|
| Q6 | LLM key | **OpenAI 官方** | Phase 3 用 OpenAI-compatible 适配器直连 api.openai.com；Anthropic 适配器后置到 Phase 7 |
| Q8 | 确定性核心语言 | **Rust** | Phase 2 我写主干并逐段解释；Brain 仍是 TS |
| Q11 | 仓库布局 | **本仓库，C# 移到 `legacy/`** | Phase 0.2 |
| Q7 | Embedding | **必须本地** | v2 默认 `fastembed` 进程内 `bge-m3`；云端 embedding 只作显式可选；引出 Q15 |
| Q15 | 隐私路由 | **接受**：`个人/` = local_only，其余 cloud_ok，`mode=drop` | 04 §1、§4；Phase 4.3b |
| Q12 | 包管理器 | pnpm 12（npm -g 安装；corepack 在本机 EPERM） | — |

## A. 直接影响 Phase 0

（全部已确认）

## B. 影响 v2 设计

### Q9 · VPet 与知识库 app 的连接方式
06 §1.3 的 A / B / C。另外：知识库 Electron 版是随机端口，需要它写一个 `.kb/port` 文件（改知识库一行代码），或者固定端口。你愿意改知识库那边吗？
**默认**：A（HTTP）+ 让知识库写 `.kb/port`。

### Q4 · 数据范围
- Obsidian：整个 `My-md/`？还是排除 `个人/`？（Q15 已经给了更细的答案：都索引，但 `个人/` 不出网）
- GitHub：哪个账号、哪些仓库？（`yixiongfei/*` 全部？）
- 日历：知识库的 schedule 就是唯一日历？还是你还在用 Google/Outlook？
- 活动窗口标题监控：默认关，你想开吗？
**默认**：`My-md/` 全部可读；GitHub 只读你自己的公开+私有仓库；schedule 是唯一日历；活动窗口关。

### Q1 · 主动程度的三道刹车（01 ①）
频率 ≤1 次/30 分钟、安静时段 23:00–08:00、番茄钟内不打断——这些数字合适吗？
**默认**：如上。

### Q2 · v1 写权限边界（01 ②）
只写 `个人/Daily/YYYY-MM-DD.md`（追加）和知识库 `schedule/event`（每次确认）。Daily 目录现在在哪？有没有模板？
**默认**：`My-md/个人/Daily/`，不存在则用 06 §2 的模板创建。

## C. 影响人格与体验

### Q3 · 人格（01 ③ 的 Character Card）
- 名字：沿用"萝莉斯"（原角色），还是"小 V"，还是新名字？
- 语言：默认中文；在日语/英语材料上下文里要不要跟着切？
- 你希望它像什么人：自习室的学长学姐？严格的私教？安静的同事？
- 对拖延/低效的态度：完全不评判？还是可以直说？
**默认**：名字"小V"、中文、"一起自习的学长/学姐"、不评判但可以直说事实。你写 5–10 行，我整理成 `personality.yaml`。

### Q5 · 最终身份
确认 C（有身体的 Personal Agent）。**默认**：C。

### Q10 · 平台
只做 Windows？**默认**：是。

### Q13 · 唤起快捷键
**默认**：`Alt+V`。

### Q14 · 原版游戏性
金钱 / 等级 / 商店 / 投喂 / 生病——全部砍掉，还是保留一部分当作"彩蛋"？
**默认**：v1–v3 全部砍掉；体力/心情保留但改由你的行为驱动。
